# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI temp (link do projeto local)
supabase/.temp/

# Segredos locais (arquivo já rastreado no histórico; isto só evita que
# futuras alterações nele sejam incluídas automaticamente em `git add -A`)
.env

# Node portátil local usado sem instalação global
.tools/

# Artefatos gerados (planilhas exportadas etc.)
outputs/

# Lockfile do npm gerado por acidente — o repo usa bun.lock
package-lock.json

# Dado sensível local (nomes/e-mails de colaboradores) — scripts que o
# consomem (ex.: scripts/diag_troca_senha.py) têm fallback se ausente
config/colaboradores_contabil.csv
